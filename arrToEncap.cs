namespace LearningcSharpOOP
{
    internal class arrToEncap
    {
   /*    //array 
        string[] num;
        num = new string[] { "volvo", "hyundai", "look" };
        Console.WriteLine(num.Length);
            foreach (string item in num)
            {
                Console.WriteLine(item);
            }
        Array.Sort(num);  //to sort array
        Console.WriteLine("---------------------------------");
        for (int i = 0; i < num.Length; i++) //.length is used to convert array string in int length
        {
            Console.WriteLine(num[i]);
        }

        //methods
        Console.WriteLine("---------------------------------");

        MyClass obj = new MyClass();
        obj.mymethod("thsiisfname");
        obj.myMethod(2, 4);
        MyClss arrayofcar = new MyClss();
        arrayofcar.mymethod();
        Car Ford = new Car();  // Create an object of the Car Class (this will call the constructor)
        Console.WriteLine(Ford.model);  // Print the value of model
        Console.WriteLine("--------------------");
        fibonaaci Encap = new fibonaaci();
        Encap.Num = 1234;
            
       }
        class MyClass
        {
            public void mymethod(string fname) //this is parameter with method 
            {

                Console.WriteLine("This is a method inside a class " + fname);
             }
             public int myMethod(int num1, int num2) //method overloading
            {

                return num1 + num2;

                }
}
class MyClss
{
    public void mymethod() //class member and class method used with public to publically avaialable in to call via object 
    {
        string[] Cars = new string[]
        {
                    "Volvo" ,"hyundai"
        };
        for (int i = 0; i < Cars.Length; i++)
        {
            Console.WriteLine(Cars[i]);
        }

    }
}
        class Car
        {
             public string model;  // Create a field

    // Create a class constructor for the Car class
    public Car()
    {
        model = "Mustang"; // Set the initial value for model
    }


}*/
    }
}
